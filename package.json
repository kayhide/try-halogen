{
  "private": true,
  "scripts": {
    "build": "pulp browserify --to dist/app.js",
    "watch": "pulp -w browserify --to dist/app.js",
    "watch-fast": "webpack --entry ./entry.js --output-filename dist/app.js --progress --watch",
    "browse": "browser-sync start --server 'dist' --files 'dist/**/*.html' 'dist/app.js'",
    "dev": "nf start"
  },
  "devDependencies": {
    "browser-sync": "^2.18.13",
    "foreman": "^2.0.0",
    "webpack": "^3.6.0"
  },
  "dependencies": {
    "aws-sdk": "^2.132.0"
  }
}
